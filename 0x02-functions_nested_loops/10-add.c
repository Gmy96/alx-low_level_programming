#include <stdio.h>
#include "main.h"

/**
* add - function that adds two integers and returns the result
*
* @x: The first intger
*
* @y: the second intger
*
* Return: The result
*/

int add(int x, int y)
{
	int z = x + y;

	return (z);
}
